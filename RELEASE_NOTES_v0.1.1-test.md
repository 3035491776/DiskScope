# DiskScope v0.1.1 朋友测试版

这是正式对外发布前的易用性测试包，不是正式 GitHub Release，也不会创建 v0.1.1 tag。

## 本次重点

- 总览直接回答 C 盘用了多少、DiskScope 扫描到了多少，以及下一步可以扫描什么。
- “Snapshot、scope、candidate、eligibility、coverage、metadata、policy”等内部概念不再出现在普通界面第一层。
- 扫描状态使用“已发现文件、已扫描文件夹、已运行、扫描速度、内存占用”等日常表达。
- 空间分析、大文件、扫描历史和空间建议使用更直白的中文，并减少同屏字段。
- 原始 ID、规则版本、错误码和资源计数仍保留在默认折叠的“技术详情”中。
- 开发测试文件、原始审计记录、Fixture 和项目工作区只在开发模式显示。

## 安全边界

扫描仍然只读取文件基本信息，不读取正文、不计算哈希、不使用 MFT 或 USN、不访问原始磁盘、不跟随系统链接、不跨磁盘，也不请求管理员权限。处理规则没有变化：只允许极少数严格合格的当前用户临时文件逐个确认后移入 Windows 回收站；没有批量处理、文件夹处理或永久删除。

## 测试方式

请完整解压 `DiskScope-v0.1.1-test-windows-x64.zip` 后双击 `DiskScope.exe`。无需安装 Python、Node.js 或其他开发工具，也不需要管理员权限。

## 已知限制

- 完整 C 盘扫描可能需要较长时间。
- 部分受保护位置可能无法扫描。
- 临时文件详细信息最多保存 10,000 项。
- 历史扫描信息不代表文件当前状态。
- Windows 11 x64 已测试；Windows 10 x64 是目标支持环境，但尚未实机验证。
- 测试包未签名，可能触发 SmartScreen 或安全软件声誉提示。
- 没有安装程序；请先完整解压 ZIP。
